Description:
    Creates a migration for adding a table used by the uvi gem.

    The generator takes a migration name as its argument.  The migration name may be
    given in CamelCase or under_score.

Example:
    ./script/generate uvi_migration AddUvi
